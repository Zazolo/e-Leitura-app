export interface historiaAll{
    id:string, titulo:string, max_ciclos:number, ciclo_atual:number, tempo_ciclo:number, finalizada:boolean, senha:string, usuario_id:{id:number, nome:string, login:string}, dthr_criacao:string
  }